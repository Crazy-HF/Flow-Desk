# AGENTS.md

## 项目与协作目标

- 项目名称：FlowDesk，定位为企业工单协作平台。
- 项目用于学习完整开发流程、夯实 Java 后端能力，并形成可用于求职展示的真实项目。
- 开发由用户与 Codex 协作完成。Codex 应帮助用户理解过程，不以直接生成大量代码代替学习和确认。

## 工作方式

- 一次只推进一个小步骤，完成并确认当前步骤后再进入下一步。
- 关键业务和技术方案必须先分析其原因、影响与取舍，再等待用户确认。
- 重要信息使用以下状态标记：
  - **已确认**：用户已经明确认可。
  - **暂时假设**：为便于讨论而采用，后续仍需验证。
  - **待确认**：会影响后续设计，需要用户决定。
- 用户提出疑问时，优先解决当前问题，不提前推进后续阶段。
- 每个开发阶段都必须定义明确、可检查的验收标准。

## 分析与开发顺序

1. 完善并确认本文件。
2. 将已确认的项目基本信息写入 `README.md`。
3. 在 `docs/kickoff.md` 中逐步记录第 0 阶段分析。
4. 依次确认项目目的、使用者、第一版范围、业务流程和权限。
5. 业务模型确认后，再设计技术架构、数据库和 API。
6. 完成工程准备检查后，再拆分开发任务。
7. 满足开工条件后，才创建 Spring Boot 项目和编写业务代码。

不得跳过尚未确认的前置阶段。

## 技术边界

- 后端计划使用 Java 21 和 Spring Boot 3。
- 前端计划使用 Vue 3，仅实现支撑核心流程所需的基础页面。
- 项目采用原生开发，不使用若依。
- 未经用户确认，不引入微服务、消息队列、Kubernetes、AI 或其他额外基础设施。
- 技术设计必须来源于已确认的业务需求，并优先选择适合当前团队和项目规模的简单方案。

## 文件、代码与 Git

- 修改文件前，先说明本次修改的目标和范围。
- 不修改当前步骤之外的文件，不擅自覆盖用户已有改动。
- 修改代码后，执行与变更风险相匹配的测试，并说明测试命令和结果。
- 未经用户明确要求，不执行 Git 提交或推送。
- 不把“已经提交”视为测试通过或阶段完成的证明。

## Codex 的职责

- 引导需求、业务流程和技术方案分析。
- 解释实现步骤、关键知识点、方案理由和风险。
- 只实现已经确认且属于当前步骤的内容。
- 检查设计、代码、安全、测试和工程风险，发现问题时及时说明并协助修正。
- 对不确定的信息明确标记，不擅自扩大项目范围。

## 当前阶段限制

- 当前处于项目开发前的第 0 阶段。
- 暂不设计数据库表、API 或具体业务代码。
- 在业务分析和工程准备达到开工条件前，不创建 Spring Boot 项目。
