# AGENTS.md

## 项目与协作目标

- 项目名称：FlowDesk，定位为企业工单协作平台。
- 项目用于学习完整开发流程、夯实 Java 后端能力，并形成可用于求职展示的真实项目。
- 开发由用户与 Codex 协作完成。Codex 应帮助用户理解过程，不以直接生成大量代码代替学习和确认。

## 工作方式

- 以一个完整主题作为推进单元，同一主题内相关且低风险的内容可以连续分析、记录和检查，不要求用户逐条确认。
- 能够由已确认内容直接推导、且不会扩大范围或改变关键行为的事项，由 Codex 直接处理，并在本轮结束时统一说明。
- 关键业务和技术方案必须先分析其原因、影响与取舍；存在分歧或需要用户决定的事项应集中列出，再由用户一次确认。
- 每个阶段原则上设置一至两个集中确认节点。遇到明显矛盾、较高返工成本、范围扩张或越过当前阶段边界时，应立即暂停并请求用户确认。
- 用户确认方案后，只需简短提示确认通过并直接继续当前阶段，不重复大段总结已经确认的内容。
- 重要信息使用以下状态标记：
  - **已确认**：用户已经明确认可。
  - **暂时假设**：为便于讨论而采用，后续仍需验证。
  - **待确认**：会影响后续设计，需要用户决定。
- 用户提出疑问时，优先解决当前问题，不提前推进后续阶段。
- 每个开发阶段都必须定义明确、可检查的验收标准。

## 分析与开发顺序

1. 完善并确认本文件。
2. 将已确认的项目基本信息写入 `README.md`。
3. 在 `docs/kickoff.md` 中逐步记录第 0 阶段分析。
4. 依次确认项目目的、使用者、第一版范围、业务流程和权限。
5. 业务模型确认后，再设计技术架构、数据库和 API。
6. 完成工程准备检查后，再拆分开发任务。
7. 满足开工条件后，才创建 Spring Boot 项目和编写业务代码。

不得跳过尚未确认的前置阶段。

## 技术边界

- 后端计划使用 Java 21 和 Spring Boot 3。
- 前端计划使用 Vue 3，仅实现支撑核心流程所需的基础页面。
- 项目采用原生开发，不使用若依。
- 未经用户确认，不引入微服务、消息队列、Kubernetes、AI 或其他额外基础设施。
- 技术设计必须来源于已确认的业务需求，并优先选择适合当前团队和项目规模的简单方案。

## 文件、代码与 Git

- 修改文件前，先说明本次修改的目标和范围。
- 不修改当前步骤之外的文件，不擅自覆盖用户已有改动。
- 修改代码后，执行与变更风险相匹配的测试，并说明测试命令和结果。
- `main` 是稳定主分支，每项工作使用 `flow-desk/<主题>` 格式的短期分支；完成检查后通过合并进入 `main`，不直接在 `main` 上持续开发。
- 在新机器或新工作目录开始开发时，先拉取并切换到最新 `main`，再按照 `PROJECT_STATUS.md` 记录的下一分支名称从 `main` 创建短期分支；不得基于已完成的旧主题分支继续开发。
- 未经用户明确要求，不执行 Git 提交或推送。
- 不把“已经提交”视为测试通过或阶段完成的证明。

## 项目状态与跨机器接续

- 仓库根目录的 `PROJECT_STATUS.md` 是快速接续入口，不替代需求、业务模型或技术设计文档。
- 新机器克隆项目、恢复中断任务或开始新会话时，先读取 `AGENTS.md` 和 `PROJECT_STATUS.md`，再按照其中的“当前任务必读”只读取与当前任务有关的详细文档；无需默认通读整个仓库。
- 在阶段完成或切换、关键决策改变后续方案、任务因阻塞暂停、工作分支准备交接或合并前，必须同步更新 `PROJECT_STATUS.md`。
- 普通拉取、没有改变项目状态的小提交、格式调整或同一任务内的常规实现，不单独更新 `PROJECT_STATUS.md`。
- 更新状态文件时保持简洁，只记录当前阶段、已完成里程碑、关键技术决策、下一步、当前任务必读文件和已知阻塞。
- `PROJECT_STATUS.md`、`README.md` 与本文件的当前阶段必须一致；若出现冲突，应先核对最近确认的阶段结论并修正文档，再继续开发。

## Codex 的职责

- 引导需求、业务流程和技术方案分析。
- 解释实现步骤、关键知识点、方案理由和风险。
- 只实现已经确认且属于当前步骤的内容。
- 检查设计、代码、安全、测试和工程风险，发现问题时及时说明并协助修正。
- 对不确定的信息明确标记，不擅自扩大项目范围。

## 当前阶段限制

- 项目启动分析、业务模型和总体架构已经完成，数据库设计尚未开始，当前等待用户指令。
- 下一步数据库设计应先完成逻辑数据模型，再确定 MySQL 物理表、约束和索引；暂不设计 API，不创建 Spring Boot 项目，也不编写业务代码。
- 后续必须完成 API 与页面交互设计、工程准备检查和任务拆分，满足开工条件后才能进入编码阶段。
